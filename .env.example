# API 认证密钥（必填）
MEMORY_API_KEY=change-me-to-a-random-secret

# 是否跳过认证（仅本地开发设为 true）
SKIP_AUTH=false

# 向量模型，默认为 all-MiniLM-L6-v2
EMBEDDING_MODEL=all-MiniLM-L6-v2

# 数据目录（容器内挂载路径）
DATA_DIR=/data

# 服务端口
PORT=33333
