# API 认证密钥（必填）
MEMORY_API_KEY=change-me-to-a-strong-secret-key

# 是否跳过认证（仅本地开发设为 true）
SKIP_AUTH=false

# 向量模型，默认为 all-MiniLM-L6-v2
EMBEDDING_MODEL=all-MiniLM-L6-v2

# 数据目录（容器内挂载路径）
DATA_DIR=/data

# 服务端口
PORT=33333

# CORS 允许的源，逗号分隔（默认 *）
CORS_ORIGINS=*

# 模型缓存目录
MODEL_CACHE=/data/model_cache
